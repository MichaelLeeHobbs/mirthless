services:
  db:
    image: postgres:17-alpine
    container_name: mirthless-db-prod
    environment:
      POSTGRES_DB: mirthless
      POSTGRES_USER: mirthless
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U mirthless -d mirthless"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    restart: unless-stopped
    networks:
      - mirthless

  server:
    build:
      context: ..
      dockerfile: docker/Dockerfile.server
    container_name: mirthless-server
    depends_on:
      db:
        condition: service_healthy
    env_file:
      - ../.env
    environment:
      DATABASE_URL: postgresql://mirthless:${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}@db:5432/mirthless
    expose:
      - "3000"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:3000/health/live || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 40s
    restart: unless-stopped
    networks:
      - mirthless

  web:
    build:
      context: ..
      dockerfile: docker/Dockerfile.web
    container_name: mirthless-web
    depends_on:
      - server
    ports:
      - "80:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://localhost:80/ || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    restart: unless-stopped
    networks:
      - mirthless

volumes:
  postgres_data:

networks:
  mirthless:
    driver: bridge
